# 快速使用指南 - 策略配置

## 🎯 如何启用/禁用策略

### 方法 1：修改配置文件

编辑 `config/strategies.json`，修改策略的 `enabled` 字段：

```json
{
  "name": "Mean Reversion (RSI)",
  "display_name": "均值回归策略",
  "module": "core.strategies.mean_reversion",
  "class_name": "MeanReversionStrategy",
  "enabled": false // 👈 改为 false 禁用，改为 true 启用
}
```

### 方法 2：查看当前启用的策略

运行验证脚本：

```bash
python tests/test_refactoring.py
```

或者：

```bash
python -c "from core.strategy_loader import get_enabled_strategies; print(get_enabled_strategies())"
```

## 📝 示例场景

### 场景 1：隐藏测试策略

如果你正在测试一个新策略，但还不想在页面显示：

```json
{
  "name": "Experimental Strategy",
  "display_name": "实验性策略",
  "module": "core.strategies.experimental",
  "class_name": "ExperimentalStrategy",
  "enabled": false // 👈 测试期间禁用
}
```

### 场景 2：只显示常用策略

如果你只想使用几个核心策略，可以禁用其他策略：

```json
{
  "strategies": [
    {
      "name": "Daily DCA",
      "enabled": true // ✅ 显示
    },
    {
      "name": "MA200 Trend",
      "enabled": true // ✅ 显示
    },
    {
      "name": "Pyramid Grid",
      "enabled": false // ❌ 隐藏
    }
  ]
}
```

### 场景 3：添加新策略

1. 创建策略文件 `core/strategies/my_strategy.py`
2. 添加配置到 `config/strategies.json`
3. 刷新应用

无需修改 `app.py`！

## ⚠️ 注意事项

1. 修改配置后需要重启应用或刷新页面
2. `module` 和 `class_name` 必须准确匹配
3. 所有策略都必须继承 `BaseStrategy`
4. 至少保留一个策略为启用状态

## 🔍 故障排查

### 问题：配置修改后没有生效

**解决方法**：

- 重启 Streamlit 应用
- 检查 JSON 语法是否正确
- 确认文件保存成功

### 问题：策略加载失败

**解决方法**：

- 运行 `python tests/test_refactoring.py` 查看详细错误
- 检查 `module` 路径是否正确
- 检查 `class_name` 是否与实际类名一致

### 问题：策略在页面不显示

**解决方法**：

- 确认 `enabled: true`
- 清除浏览器缓存
- 检查控制台是否有报错

## 📚 更多信息

- [配置示例](./策略配置示例.md)
