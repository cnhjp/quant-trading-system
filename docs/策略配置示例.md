# 策略配置示例

## 示例 1：禁用测试策略

如果你有一个仅用于测试的策略，不想在生产环境显示，可以这样配置：

```json
{
  "name": "Mean Reversion (RSI)",
  "display_name": "均值回归策略",
  "module": "core.strategies.mean_reversion",
  "class_name": "MeanReversionStrategy",
  "enabled": false // 👈 设置为 false，该策略将不会加载
}
```

## 示例 2：添加新策略

### 步骤 1：创建策略文件

在 `core/strategies/` 下创建 `my_custom_strategy.py`：

```python
import pandas as pd
import numpy as np
from .base import BaseStrategy

class MyCustomStrategy(BaseStrategy):
    def __init__(self):
        super().__init__("My Custom Strategy")

    def generate_signals(self, df: pd.DataFrame, **kwargs) -> pd.DataFrame:
        """
        自定义策略逻辑
        """
        signals = pd.DataFrame(index=df.index)
        signals['Signal'] = 0

        # 你的策略逻辑
        # ...

        return signals
```

### 步骤 2：添加到配置文件

在 `config/strategies.json` 中添加：

```json
{
  "name": "My Custom Strategy",
  "display_name": "我的自定义策略",
  "module": "core.strategies.my_custom_strategy",
  "class_name": "MyCustomStrategy",
  "enabled": true
}
```

### 步骤 3：更新 **init**.py（可选）

在 `core/strategies/__init__.py` 中添加导出：

```python
from .my_custom_strategy import MyCustomStrategy

__all__ = [
    # ... 其他策略
    'MyCustomStrategy',
]
```

## 示例 3：临时测试策略

在开发新策略时，可以先设置为禁用，开发完成后再启用：

```json
{
  "name": "Experimental MACD",
  "display_name": "实验性 MACD 策略",
  "module": "core.strategies.experimental_macd",
  "class_name": "ExperimentalMACDStrategy",
  "enabled": false // 👈 开发期间禁用
}
```

## 完整配置示例

```json
{
  "strategies": [
    {
      "name": "Daily DCA",
      "display_name": "每日定投策略",
      "module": "core.strategies.daily_dca",
      "class_name": "DailyDCAStrategy",
      "enabled": true
    },
    {
      "name": "MA200 Trend",
      "display_name": "均线趋势策略",
      "module": "core.strategies.ma200_trend",
      "class_name": "MA200TrendStrategy",
      "enabled": true
    },
    {
      "name": "Test Strategy",
      "display_name": "测试策略",
      "module": "core.strategies.test_strategy",
      "class_name": "TestStrategy",
      "enabled": false
    }
  ]
}
```

## 注意事项

1. 修改配置文件后需要重启应用（刷新页面）
2. `module` 路径要使用点号分隔
3. `class_name` 必须与策略类名完全一致（区分大小写）
4. `name` 字段用于内部标识，建议使用英文
5. `display_name` 用于页面显示，可以使用中文
